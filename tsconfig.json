{
  "compilerOptions": {
    "target": "ES2017", // コンパイルターゲットバージョン
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "allowJs": true, // JavaScript ファイルを許可する
    "skipLibCheck": true, // ライブラリファイルのチェックをスキップする
    "strict": true, // 厳密な型チェックを有効にする
    "noEmit": true, // コンパイル結果を出力しない（Next.jsではbabelでコンパイルする）
    "incremental": true, // 増分コンパイルを有効にする
    "module": "ESNext", // ESNextモジュールシステムを使用する
    "esModuleInterop": true, // デフォルトで非ES6モジュールをインポートできるようにする
    "moduleResolution": "node", // Node.jsのモジュール解析戦略を使用する
    "resolveJsonModule": true, // JSONモジュールのインポートをサポートする
    "isolatedModules": true, // 各ファイルを独立したモジュールとして処理する
    "jsx": "preserve", // React 17の新しいJSX変換方式を使用する
    "plugins": [
      {
        "name": "next"
      }
    ],
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts", // Next.js環境型宣言を取り込む
    ".next/types/**/*.ts", // Next.js内部生成型宣言を含む
    "**/*.ts", // すべてのTypeScriptファイルを含む
    "**/*.tsx" // すべてのTypeScript JSXファイルを含む
  ],
  "exclude": [
    "node_modules" // node_modulesディレクトリを除外する
  ]
}