/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,ts,jsx,tsx}", // 确保扫描所有组件和页面文件
    ],
    theme: {
      extend: {}, // 自定义配置可以放在这里
    },
    plugins: [],
  };